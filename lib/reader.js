'use strict';

const fs = require ('fs');

const  fileReader = module.exports = {};

fileReader.readTheArrayAsync = (paths, callback) => {
    if (error){
        cosole.log('__ERROR__ Could not read');
        return callback(error);
        }
}
return callback(data.toString('utf8'))